export const config = {
    prefix: "!",
    token:"<TOKEN>",
    initialMessage: "**React to the messages below to receive the associated role. If you want to delete the role, simply delete your reaction!**"
}